import React from 'react';
import PropTypes from 'prop-types';
import {
  Text,
  View,
} from 'react-native';

class MultiTracker extends React.Component {
  render() {
    return (
      <View>

      </View>
    );
  }
}

MultiTracker.propTypes = {

};

export default MultiTracker;
